import React from 'react';
import useCreatePlace from '../hooks/useCreatePlace';

jest.mock('axios');

describe('Private Route Dispatch', () => {

  test('should go to loading state', () => {
    // const [state, createPlace] = useCreatePlace();
    // createPlace({});
    // expect(state).toBe('loading');
  });

});